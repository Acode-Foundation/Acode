import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.shashidao.bract',
  appName: 'Bract',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
